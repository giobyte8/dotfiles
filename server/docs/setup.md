# Initial setup

Setup a fresh install of Ubuntu Server to be used as a *home server* instance.

## Base user and packages

- Base user (?)
- Latest updates & build-tools
- zsh
- docker & docker compose
- dotfiles clone and setup