#
# Use this image to run galler-dl into its own container

FROM python:3.10.2-alpine3.14

# dl: Short for downloads ;)
WORKDIR /dl

RUN pip install --no-cache-dir gallery-dl
ENTRYPOINT ["gallery-dl"]

